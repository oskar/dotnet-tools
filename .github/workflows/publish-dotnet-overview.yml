name: "Publish dotnet-overview to NuGet"
on:
  push:
    tags: ["dotnet-overview-v*"]
permissions:
  contents: read
jobs:
  publish:
    uses: ./.github/workflows/_publish-tool.yml
    with:
      csproj: src/DotNetOverview/DotNetOverview.csproj
    secrets: inherit
